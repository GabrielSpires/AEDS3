#include <stdio.h>
#include <stdlib.h>
#include "limits.h"

void calcularDist(short int*, int, int*, int*);

void gerarCombinacoes(int*, int*, int*, int, int, int, int, int*);

void fb(int, int, int*);